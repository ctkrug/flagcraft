{
  "name": "flagcraft",
  "version": "0.1.0",
  "private": true,
  "description": "Paste your CLI's --help output and get a structured lint report against the Command Line Interface Guidelines.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "typescript": "^5.6.2",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  }
}
